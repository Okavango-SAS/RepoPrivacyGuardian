# Reference only: Repo Privacy Guardian does not auto-load this file.
# Export these variables explicitly in your shell when needed.

# Optional GitHub hardening authentication.
# Use one token source, not all three.
# Complete --audit-github-hardening coverage needs GitHub permissions for
# repository Administration(read) plus security-alert/Dependabot-alert reads.
REPO_PRIVACY_GUARDIAN_GITHUB_TOKEN=
GITHUB_TOKEN=
GH_TOKEN=
