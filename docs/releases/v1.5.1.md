# Repo Privacy Guardian v1.5.1

## Summary

`v1.5.1` is a refactor-closure and release-readiness patch for the stable `1.5.x` line. It completes the remaining GUI card-spec extraction, keeps the public compatibility facade intact, expands regression coverage around target discovery, tooling preflight, redaction, and GUI assets, and records the release evidence needed for a clean public tag.

## User-visible changes

- CLI: no flag, default, exit-code, report-schema, policy-key, or remediation-default changes.
- Packaging / install: package metadata now reports `1.5.1`; wheel and source distribution install smoke paths pass.
- GUI: Settings, Reports, Prompts, Repair gate/list-shell, and repository empty-state containers now use shared specs while preserving the staged Audit -> Repair workflow and existing layout.
- Documentation: README, changelog, versioning notes, and these release notes describe the `1.5.1` patch scope.

## Validation evidence

- `python -m ruff check .`
- `pyright -p pyrightconfig.json`
- `python -m pytest -q`
- `python scripts/check_release_contract.py`
- `python tests/release_smoke_gui.py`
- `python scripts/release_readiness.py --skip-self-audit`
- Self-audit dogfood with `--audit-github-hardening --accept-github-admin-bypass --agent-summary`: PASS.
- GitHub automatic push CI for the release-prep commit: success.
- GitHub Dependency Graph update for the release-prep commit: success.

## Upgrade notes

- Changed defaults: none.
- Changed flags or outputs: none.
- Migration or operator action required: none for existing `1.5.x` users.

## Known limitations at release time

- GitHub hardening checks remain read-only and token-permission dependent.
- GUI remains a desktop companion for manual review; CLI remains the preferred automation path for agentic workflows.
- The solo-maintainer administrator branch-protection bypass is documented as an accepted risk for this repository's operating model.

## Follow-up candidates

- Keep future feature work focused on user-visible audit, report, repair, or hardening value rather than further meta-only refactors.
- Continue watching `core.py` size only when a behavior-bearing boundary becomes clearer and testable.
